<h1>Log in</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:session, url: stafflogin_path) do |f| %>

      <%= f.label :登録したメールアドレス %>
      <%= f.email_field :email, class: 'form-control', id: 'staff_email' %>

      <%= f.label :パスワード %>
      <%= f.password_field :password, class: 'form-control', id: 'staff_password' %>
      
      <%= f.label :remember_me, class: "checkbox inline" do %>
        <%= f.check_box :remember_me %>
        <span>この端末に記憶する</span>
      <% end %>

      <%= f.submit "Log in", class: "btn btn-primary" %>
    <% end %>

    <p>スタッフ登録されていない場合は? <%= link_to "こちらから登録してください", signup_path %></p>
  </div>
</div>